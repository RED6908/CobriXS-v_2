version: '3.8'

services:
  backend:
    build: .
    ports:
      - "4000:4000"

    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
      - NODE_ENV=development

    volumes:
      - .:/app
      - /app/node_modules