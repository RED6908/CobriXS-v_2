# Nueva base de datos (staging Supabase)
SUPABASE_URL=https://staging.supabase.co

# Nueva llave de acceso (staging)
SUPABASE_ANON_KEY=staging-key

# Entorno de ejecución
NODE_ENV=staging

# Puerto del backend
PORT=4000